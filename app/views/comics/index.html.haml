.main--wrapper
  .container--comics
    %h2 漫画一覧
    %p.sentence 画像をクリックすると名場面一覧が表示されます。
    .container--comics__list.clearfix
      - @comics.each do |comic|
        = link_to comic_scenes_path(comic) do
          .comic--content
            = image_tag comic.image_url,size:"200x250"
            %h3
              = comic.title
            